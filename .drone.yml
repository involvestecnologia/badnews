pipeline:
  production-push:
    image: node:alpine
    commands:
      - echo $${DOCKER_PASSWORD}
      - echo $${REGISTRY_ADDR}
      - echo $${DOCKER_USERNAME}
    secrets: [ docker_password, registry_addr, docker_username ]
  